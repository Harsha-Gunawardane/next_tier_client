import { CardHeader } from "@chakra-ui/react";
import DCard from "./Default";

const DMiniStat = ({ children, ...props }) => {
	return (
		<DCard>
			<CardHeader>Hello</CardHeader>
		</DCard>
	);
};

export default DMiniStat;
